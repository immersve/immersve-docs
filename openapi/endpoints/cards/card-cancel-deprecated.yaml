post:
  deprecated: true
  tags:
    - card
  summary: Cancel a card synchronously
  description: Cancel a card synchronously (deprecated)
  parameters:
    $ref: '../../models/card-id-path.yaml'
  responses:
    "200":
      description: Successful operation