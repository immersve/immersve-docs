type: object
required:
  - cardProgramId
  - fundingSourceId
properties:
  cardProgramId:
    type: string
    description: Id of card program to use when creating a card
  fundingSourceId:
    type: string
    description: Id of the Funding Source the card will authorize against
