get:
  tags:
    - assets
  summary: Get Balance
  description: This endpoint returns the balance and available balance for a given asset.
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: '../models/get-balance-response.yaml'
