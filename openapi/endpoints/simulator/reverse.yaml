post:
  tags:
    - simulator
  summary: Reverse a transaction
  description: This endpoint can be used to test reversing a transaction on a simulated card network
  requestBody:
    content:
      application/json:
        schema:
          $ref: './models/reverse-request.yaml'
    required: true
  responses:
    200:
      content:
        application/json:
          schema:
            $ref: './models/simulator-response.yaml'
