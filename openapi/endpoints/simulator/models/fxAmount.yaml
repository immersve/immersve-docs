type: object
description: fxAmount can be specified to process the transaction in a currency other than USD
required:
  - amount
  - currencyCode
properties:
  amount:
    type: string
    description: Transaction amount in a different currency. This can be generated by converting the `transactionAmount`
  currencyCode:
    type: string
    description: ISO 4217 currency code
