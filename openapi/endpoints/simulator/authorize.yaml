post:
  tags:
    - simulator
  summary: Authorize a transaction
  description: This endpoint can be used to test authorizing a transaction on a simulated card network
  parameters:
    - name: useMinorUnits
      in: query
      description: When enabled, transactionAmount and fxAmount.amount in request body will be accepted in minor units, an integer in the smallest denomination for the given currency. 
      required: false
      schema:
        type: boolean

  requestBody:
    content:
      application/json:
        schema:
          $ref: './models/authorize-request.yaml'
    required: true
  responses:
    200:
      content:
        application/json:
          schema:
            $ref: './models/simulator-response.yaml'
