get:
  tags:
    - assets
  summary: User Wallet Assets
  description: Gets the current logged in user's wallet assets.
  responses:
    '200':
      content:
      application/json:
        schema:
        $ref: '../openapi/models/user-assets-response.yaml'
