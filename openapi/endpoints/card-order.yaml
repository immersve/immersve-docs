post:
  tags:
    - card
  summary: Order a card
  description: |
    Order a virtual credit card for the current user
  operationId: orderCard
  requestBody:
    content:
      application/json:
        schema:
          type: object
          required:
            - transactionHash
          properties:
            amount:
              type: string
              description: The value requested to be loaded to the card
            currency:
              type: string
              description: Coin being used currently only USDC
            transactionHash:
              type: string
              description: The hash of transaction transferring USDC to Immersve Payment Protocol smart contract
    required: true
  responses:
    "200":
      description: Successful operation
      content:
        application/json:
          schema:
            $ref: "../models/card.yaml"