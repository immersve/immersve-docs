---
// Renders nothing. Registered for the Starlight slots this site does not
// use: SkipLink, Banner, Hero, Sidebar (PageFrame renders the navigation
// itself) and PageTitle (the title is rendered inside Prose so it picks up
// the prose typography).
import type { Props } from '@astrojs/starlight/props';
---
