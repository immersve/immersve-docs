tags: [ cards ]
summary: Card Expiry Upcoming
operationId: card-expiry-upcoming
description:
  |
  Triggered when a card is approaching its expiry date.
  - Virtual cards: two weeks before expiry.
  - Physical cards: six weeks before expiry.
requestBody:
  content:
    application/json:
      schema:
        allOf:
          - type: object
            properties:
              topic:
                type: string
                enum: ["card-expiry-upcoming"]
              payload:
                type: object
                properties:
                  cardholderAccountId:
                    type: string
                    example: "992b1b0f0cb8cca355c70ce08150a20d"
                    description: |
                      The Immersve cardholder's account ID.
                  cardId:
                    type: string
                    example: "0485464bedfde163eace0ece2e3e2d75"
                    description: |
                      ID of the card.
                  expiresAt:
                    type: string
                    example: "2026-12-31T00:00:00.000Z"
                    description: |
                      The date and time at which the card expires.
                  cardType:
                    type: string
                    example: "virtual"
                    description: |
                      The type of card.
                    enum:
                      - virtual
                      - physical

          - $ref: "./webhook-envelope.yaml"
