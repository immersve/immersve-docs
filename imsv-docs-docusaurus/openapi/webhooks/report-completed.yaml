tags: [ reports ]
summary: Report Completed
operationId: report-completed
description: |
  Triggered when a report is completed and ready for download.

requestBody:
  content:
    application/json:
      schema:
        allOf:
          - type: object
            properties:
              topic:
                type: string
                enum: [report-completed]
              payload:
                type: object
                properties:
                  reportType:
                    type: string
                    enum:
                      - daily-funding-balance-report
                      - monthly-funding-balance-report
                      - daily-funding-source-interaction-report
                      - monthly-funding-source-interaction-report
                      - daily-payment-report
                      - monthly-payment-report // CHANGE HTIS OSCAR
                    description: |
                      The type of report being generated.
                  reportId:
                    type: string
                    description: |
                      Identifier of the report.
                    example: report-1234

          - $ref: './webhook-envelope.yaml'
