tags: [ kyc ]
summary: KYC Succeeded
operationId: kyc-succeeded
description: |
  Triggered when a cardholder's KYC status is verified and a `passed` result occurs.
requestBody:
  content:
    application/json:
      schema:
        allOf:
          - type: object
            properties:
              topic:
                type: string
                enum:
                  - "kyc-succeeded"
              payload:
                type: object
                properties:
                  kycStatus:
                    type: string
                    example: "succeeded"
                    description: |
                      Verification completed successfully

                  accountId:
                    type: string
                    example: "e8e48f309cfc11f08711"
                    description: |
                      The Immersve cardholder's account id.

          - $ref: './webhook-envelope.yaml'
