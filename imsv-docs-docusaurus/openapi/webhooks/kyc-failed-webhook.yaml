tags: [ kyc ]
summary: KYC Failed
operationId: kyc-failed
description: |
  Triggered when a cardholder's associated KYC Profile status is verified and a `failed` result occurs.
requestBody:
  content:
    application/json:
      schema:
        allOf:
          - type: object
            properties:
              topic:
                type: string
                enum: ["kyc-failed"]
              payload:
                type: object
                required:
                  - kycStatus
                  - accountId
                properties:
                  kycStatus:
                    type: string
                    example: "failed"
                    description: |
                      Verification rejected

                  accountId:
                    type: string
                    example: "e8e48f309cfc11f08711"
                    description: |
                      The Immersve cardholder's account id.

          - $ref: './webhook-envelope.yaml'
