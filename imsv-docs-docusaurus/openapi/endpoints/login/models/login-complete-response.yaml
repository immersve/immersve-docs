type: object
properties:
  accessToken:
    type: string
    description: The access token that will be used for subsequent authenticated requests.
    example: "eyJhbGasddweUzI1NiIsI..."
  refreshToken:
    type: string
    description: JWT that is used to request a new access token. Refresh tokens are valid for 30 days.
    example: "eyJhbGciOiJSUzI1NiIsI..."
  cardholderAccountId:
    type: string
    description: The unique identifier of the cardholder.
    example: "29097f50d221858223a17633e36716f9"
