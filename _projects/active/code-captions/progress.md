# Progress: Code Captions

**Current Status:** Initiated. Project files reviewed and
committed; ready to begin Stage A1 and Stage B1.

## Last Session (2026-06-13)

- Completed: Authored `brief.md`, `plan.md`, `progress.md`,
  `decisions.md` on branch `code-captions-kickoff` off
  `origin/main`. Reframed scope after review to make captions
  themselves the deliverable (form — title or paragraph — is a
  per-block choice).
- Outcome: Project scaffolding committed; no source changes yet.

## Next Session

- Pick up from: branch `code-captions-kickoff` in the project
  worktree; project files under
  `_projects/active/code-captions/`.
- First task: begin **Stage A1** (locate existing caption
  implementation in the Astro/Markdoc integration) and **Stage
  B1** (enumerate every fenced code block under
  `imsv-docs-astro/src/content/docs/` and propose a caption per
  block — title or paragraph form — as a single reviewable
  artifact checked into the project folder). Stage B1's output is
  itself a review gate before any source files are edited.
- Expected delivery shape: a single PR at the end, not a batched
  rollout. Plan currently reflects that.
